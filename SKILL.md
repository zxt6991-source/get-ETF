---
name: get-etf
description: 行业/主题 ETF 的获取、筛选、对比与挑选。用户给出赛道关键词（消费、新能源、半导体、医药、红利、军工、人工智能等）或具体 ETF 代码，想要「挑一只 ETF」「这几只 ETF 哪个好」「看好某个赛道买什么」「ETF 打分/排名/对比表」「按编制信息+行业分布+历史表现+估值挑 ETF」时使用。内含四步分析法（编制信息解构 / 行业穿透与集中度 / 历史表现验证 / 价值与估值预判）、阈值评分卡、Markdown 报告模板、HTML 富交互报告规范与实例、纯离线打分脚本。不负责：个股选股、下单交易、实时盯盘、基金申赎操作。
---

# 获取与挑选行业/主题 ETF

一套「从赛道关键词 → 候选 ETF 池 → 结构化打分 → 挑选结论」的完整流程。
方法论来源：`references/framework.md`（四步分析法原文提炼），案例见 `references/case-consumer-etf.md`。

## 何时使用

- 「我想买消费/新能源/AI 赛道的 ETF，买哪只？」
- 「这几只 ETF 有什么区别，怎么选？」
- 「帮我按编制信息、行业分布、历史表现、估值做一个 ETF 对比」
- 「给这些 ETF 打分排序，输出一份挑选报告」
- 「出一份带图表、可分享的 ETF 挑选报告（网页 / HTML 版）」

不适用：个股择时、基金定投计划执行、交易下单、境外 ETF 跨境税务建议。

## 输入确认（缺失则先问，不要自行编造赛道）

| 输入 | 说明 | 默认 |
| --- | --- | --- |
| 赛道关键词 | 行业（消费/医药/军工）或主题（AI/机器人/低空经济） | 必填 |
| 投资约束 | 偏好高弹性 or 均衡分散；是否要股息；持有期 | 未给则报告里并列给「不同偏好对应选择」 |
| 候选范围 | 已有代码清单 / 需要自己筛 | 需要自己筛 |
| 输出形态 | 对话内表格、Markdown 文件、**HTML 富交互报告**、飞书文档/表格 | 对话内报告 |

## 主流程（6 步，不可跳步）

### 第 0 步：锁定赛道边界
写清赛道定义：属于哪个一级行业、是否跨二级行业、是否含港股/海外。主题 ETF（如 AI、机器人）务必先明确「主题成分的判定规则」，否则候选池会失控。

### 第 1 步：构建候选池
1. 按关键词检索基金名称，同时检索**跟踪指数名称**（很多 ETF 名称里没有关键词，例如「券商 ETF」跟踪的是「中证全指证券公司指数」）。
2. 去重：同一指数被多只 ETF 跟踪时全部保留（用于比较规模、费率、跟踪误差）。
3. 硬性剔除：① 规模 < 1 亿元；② 上市不足 6 个月（历史表现不可比，可标注例外）；③ 已公告清盘/终止上市。
4. 产出「候选池表」：ETF 代码 / 名称 / 跟踪指数 / 规模 / 平均日成交额 / 费率。

### 第 2 步：指数编制解构（看底层逻辑）
对候选池里每一个**不同的跟踪指数**，逐一填 4 个要素：
- **选样规则**：市值/成交额/基本面因子？抽样复制还是全部纳入？母指数范围（如"上证 180 内"）？
- **加权方法**：自由流通市值加权（贴近真实交易）/ 等权重（放大中小盘）/ 其他。
- **调仓机制**：频率 + 数量。高频调仓更贴合市场但换手成本高；低频调仓稳定。
- **个股约束**：成分股数量 + 单股权重上限（≤5% 分散 / ≥15% 易被龙头主导，弹性与风险同步放大）。

### 第 3 步：行业穿透与集中度（锚定风险收益特征）
- **二级行业穿透**：不能只看一级行业。新能源要拆锂电池/光伏/风电；消费要拆白酒/家电/食品/农林牧渔/医药。
- **权重股集中度**：前十大权重股占比 + 第一大个股权重。> 50% 即被少数龙头主导，需权衡机会与风险。
- **同业对照**：把同赛道指数的行业权重做横向表，才能看出"均衡"还是"押注单一板块"。

### 第 4 步：历史表现验证
- **收益**：1 年 / 3～5 年 / 5 年以上，分段看牛、震荡、熊市表现，判断收益弹性（尽量按季度滚动看，而非只看区间累计）。
- **风险**：最大回撤（抗跌能力）、年化波动率（波动强度）。
- **信息比率**：衡量承担同等主动风险下跑赢基准的能力，同赛道横向比较策略有效性。

### 第 5 步：价值与估值预判
- **估值分位**：当前 PE / PB 在历史区间的分位（注意口径与起始年份）。
- **盈利增长**：机构对成分股的盈利增速预测，看**边际变化**而非绝对值。
- **股息率**：高股息 = 安全边际，红利类指数尤其要看。
- 结论：高股息 + 盈利提速 + 估值低位 = 「收益 + 安全」双重优势。

### 第 6 步：打分排序 + 匹配投资目标
1. 用 `scripts/etf_screen.py` 对候选池打分（五维度加权，缺失维度自动归一化）。
2. 按投资者偏好给结论（同时给多个备选，而不是只给一个答案）：
   - 追求弹性 → 集中度高、单一强势板块权重高的指数
   - 追求均衡 → 前十大占比低、跨二级行业分散的指数
   - 追求性价比 → 估值分位低 + 集中度中等
   - 追求防御 → 高股息、低波动、低回撤
3. 输出报告：
   - 常规报告 → 模板见 `references/report-template.md`；
   - **要图表、要分享的精美报告 → 直接借鉴现成实例 `examples/消费ETF挑选报告-20260919.html`**，规范见 `references/html-report-guide.md`（页面骨架、设计 token、5 张图表规格、决策矩阵卡片写法、来源表与免责声明）。

## 输出形态选择

| 用户想要 | 用 | 参考 |
| --- | --- | --- |
| 只要结论/排名 | 对话内 Markdown 表格 | `report-template.md` |
| 可分享、带图表、要存档 | **HTML 富交互报告** | **`examples/消费ETF挑选报告-20260919.html`（照抄改数据）** + `html-report-guide.md` |
| 团队协作、要持续更新 | 飞书文档 / 表格 / 多维表格 | `lark-doc` / `lark-sheets` / `lark-base` |

HTML 实例的要点（详见 `html-report-guide.md`）：
- **九节结构**：结论先行（4 张"偏好 → 标的"决策卡片）→ 综合评分 → 候选池 → 编制解构 → 行业穿透与集中度 → 历史表现 → 估值 → 风险与预警 → 数据来源；
- **红涨绿跌**配色，主色 `#1f5fa8`；KPI 四卡（候选数 / 入选数 / 赛道估值水位 / 赛道近 1 年表现）；
- 5 张图：综合分横向条形、集中度分组柱、收益正负染色柱、风险收益散点（右下=好）、估值分位+股息双轴；
- 空缺数据在图上画 `null` **留缺口**、表里写"待补"，并在风险节说明对打分的影响；
- **注意**：实例用 CDN 加载 ECharts，离线打开会没图。用 `create_document` 产出对话内 HTML 时必须去掉外部脚本（改内联 SVG / CSS 条形图或降级为表格）；落盘分享则把 `echarts.min.js` 内联或本地化。

## 阈值速查（评分卡口径）

| 指标 | 好 | 中 | 差 / 预警 |
| --- | --- | --- | --- |
| 规模 | > 20 亿 | 5～20 亿 | < 1 亿（剔除） |
| 平均日成交额 | > 2 亿 | 0.2～2 亿 | < 0.05 亿（流动性风险） |
| 前十大权重占比 | ≤ 30% | 30%～55% | > 75%（个股主导） |
| 第一大行业权重 | ≤ 40% | 40%～60% | > 75%（单一板块押注） |
| 单股权重上限 | ≤ 5% | 5%～15% | 无上限 / > 15% |
| 最大回撤 | ≤ 20% | 20%～40% | > 50% |
| 年化波动率 | ≤ 15% | 15%～25% | > 30% |
| 信息比率 | ≥ 0.7 | 0.3～0.7 | ≤ 0 |
| PE/PB 分位 | ≤ 20% | 20%～60% | > 80%（高估预警） |
| 股息率 | ≥ 4% | 2%～4% | < 1% |

## 数据规则（硬性）

1. **每个数字必须带「数值 + 数据日期 + 来源」**；拿不到就写「待补」，绝不编造或凭记忆填。
2. 规模/成交额优先用最近一次季报规模 + 近 20 个交易日均成交额，并注明时点。
3. 估值分位必须注明「口径 + 起始年份」，且提醒用户：**成分股调整会让历史估值分位失真**。
4. 盈利预测必须提醒样本偏差：可能只由少数成分股/单一券商覆盖构成。
5. 交叉校验：关键数字至少两处来源一致（基金公司公告 vs 指数公司官网 vs 第三方行情站）。
6. 结尾固定免责声明：本文为数据整理与分析，不构成投资建议。

详细数据源清单与获取方式见 `references/data-sources.md`。

## 文件导航

```
get-etf/
├── SKILL.md                          # 本文件：流程与规则
├── references/
│   ├── framework.md                  # 四步分析法详解（原理层）
│   ├── scoring-card.md               # 评分卡权重与分档说明
│   ├── data-sources.md               # 数据源、字段口径、校验方法
│   ├── report-template.md            # Markdown 报告模板（9 节）
│   ├── html-report-guide.md          # HTML 富交互报告规范（骨架/token/图表/卡片）
│   └── case-consumer-etf.md          # 消费 ETF 案例（4 个指数对比）
├── scripts/etf_screen.py             # 离线打分脚本（纯标准库）
└── examples/
    ├── consumer-etf-candidates.csv   # 示例输入：候选池
    ├── consumer-etf-report.md        # 示例输出：脚本生成的 Markdown 打分报告
    ├── 消费ETF挑选报告-20260919.html  # ★ 示例输出：完整 HTML 富交互报告（交付样板，可照抄改数据）
    └── build_example.py              # 生成示例候选池 CSV
```

## 脚本用法

```bash
# 生成输入模板
python3 scripts/etf_screen.py --template > candidates.csv

# 打分并输出 Markdown 报告（候选池 CSV → 排名 + 分维度明细 + 预警）
python3 scripts/etf_screen.py examples/consumer-etf-candidates.csv --format markdown

# 输出 JSON，便于接入飞书表格 / 后续 workflow
python3 scripts/etf_screen.py candidates.csv --format json --out result.json
```

字段说明见 `references/scoring-card.md`。缺失字段留空即可，脚本会自动归一化并在报告里标注「数据覆盖率」。

## 常见误区

- 只看名字不看跟踪指数 → 同名/近名 ETF 行业暴露可能完全不同。
- 用规模大当"好" → 规模只解决流动性与清盘风险，不解决风险收益特征。
- 只看累计收益 → 必须配最大回撤 / 波动率 / 信息比率一起看。
- 迷信估值分位 → 成分股调整 + 起始年份选择都会造成假低估/假高估。
- 忽略费率与跟踪误差 → 同指数多只 ETF 时，这是唯一差异，必须比。
- 只给一个答案 → 应给「偏好 A → 选 X；偏好 B → 选 Y」的决策矩阵。
